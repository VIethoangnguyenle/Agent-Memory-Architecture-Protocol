# AMAP CLI package
__version__ = "3.0.0"

# AMAP framework/protocol version stamped into resolved-config.yaml and the
# render context. Distinct from __version__ (the CLI package version).
FRAMEWORK_VERSION = "3.0"
